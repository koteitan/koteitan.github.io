// CodeMirrorエディタの初期化var codeMirrorEditor = CodeMirror(document.getElementById('codemirror-container'), {    mode: "javascript",    theme: "darcula",});function updateHighlight() {    var code = document.getElementById('input-code').value;    // PrismJSとHighlight.jsの更新    document.getElementById('prismjs-code').textContent = code;    document.getElementById('highlightjs-code').textContent = code;    // Highlight.jsのコードブロックをハイライト    document.querySelectorAll('pre code.hljs').forEach((block) => {        hljs.highlightBlock(block);    });    // Prism.jsのコードブロックのみを明示的にハイライト    Prism.highlightAllUnder(document.getElementById('prism-code-container'), false);    // CodeMirrorの更新    codeMirrorEditor.setValue(code);}// ページの読み込みが完了したらハイライトを更新window.onload = function() {    updateHighlight();};// デフォルトのコードスニペットを設定var defaultCodeSnippet = `function greet(name) {    console.log('Hello, ' + name + '!');}greet('World');`;// Prism.jsのテーマを変更する関数document.getElementById('prism-theme-selector').addEventListener('change', function() {    var selectedTheme = this.value;    document.getElementById('prism-theme-link').href = `https://cdn.jsdelivr.net/npm/prismjs/themes/${selectedTheme}`;});// Highlight.jsのテーマを変更する関数document.getElementById('highlight-theme-selector').addEventListener('change', function() {    var selectedTheme = this.value;    document.getElementById('highlight-theme-link').href = `https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.3.1/styles/${selectedTheme}`;});